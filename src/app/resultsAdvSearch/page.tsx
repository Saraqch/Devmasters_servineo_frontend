import React from 'react';

// Minimal stub page to satisfy Next.js app router type imports.
// This page was empty and caused the build to fail with "not a module".
// Replace with the real implementation if needed.

export default function ResultsAdvSearchPage() {
	return <div />;
}

export const runtime = 'edge';
